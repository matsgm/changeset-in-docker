{
  "name": "changeset",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "changeset": "npm exec @changesets/cli",
    "changeset:version": "npm exec @changesets/cli version"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@changesets/cli": "^2.25.0"
  }
}
